module.exports = {
    msgs : {
        resSucc : "User's data succeded server validations.",
        serverErr : "Internal server error.",
        noPage : "Page not found.",
        noData : "No data found.",
        retry : "Unable to proceed furture, please retry.",
    },
    const : {
        passwordRegex : "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{8,}$"
    }
}